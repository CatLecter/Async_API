version: '3'

services:
  tests:
    build: .
    container_name: tests

volumes:
  postgres_data:
  elasticsearch_data:
  static_volume:
  media_volume:

networks:
  movie_network:
    driver: bridge
